## Instructions

### Application setup

Run the following commands to set up the application (This was tested on Python versions 3.9 and 3.10)
```shell
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Running the application
```shell
python main.py
```